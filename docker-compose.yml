version: "5.1.2"

services:
    mariadb:
        build: srcs/requirements/mariadb/Dockerfile
        restart: always
        environment:
        -   .env
        volumes:
           - myvalumes
        ports:
            - 3306:3306
        network:
        -   inception
    worpress:
        build: srcs/requirements/worpress/Dockerfile
        restart: always

        environment:
        -   .env
    nginx:
    

